# Clario Backend Requirements
# Python 3.11+
# Pinned to the exact versions verified in the local venv (reproducible builds).

# Web Framework
fastapi[standard]==0.140.13
uvicorn[standard]==0.51.0
gunicorn==26.0.0

# AI/LLM
google-genai==2.14.0
langchain-core==1.5.2
langchain-google-genai==4.3.2

# Auth
PyJWT==2.13.0
email-validator==2.3.0

# Utilities
python-dotenv==1.2.2
python-multipart==0.0.32
httpx==0.28.1

loguru==0.7.3


# Database — Supabase (optional: falls back to SQLite if env vars not set)
# 2.15+ supports httpx>=0.28 (required by google-genai 1.69+)
supabase==2.31.0
